using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FinalNotes : MonoBehaviour
{
    /*
     * TOGGLE INSTRUCTIONS + RELOAD SCENE
     * 
     * public class Menu : MonoBehaviour
     * {
     *      public GameObject instructions;
     *      
     *      void Update()
     *      {
     *          if (Input.GetKeyDown(KeyCode.i))
     *          {
     *              instructions.SetActive(!instructions.activeSelf);
     *                  //toggles instructions when i is pressed
     *          }
     *          else if (Input.GetKeyDown(KeyCode.R))
     *          {
     *              SceneManager.LoadScene(SceneManager.GetActoveScene().name);
     *                  //reloads scene when r is pressed
     *          }
     *      }
     * }
     * 
     *      - attach menu script to menu gameObject
     *      - drag instructions UI image to instructions field of script
     * 
     */
}
